import styled from "styled-components";
import { flexCenter } from "../../style/common";

const Wrapper = styled.div`
  ${flexCenter}
  margin-top:100px;
`;

const S = { Wrapper };

export default S;
